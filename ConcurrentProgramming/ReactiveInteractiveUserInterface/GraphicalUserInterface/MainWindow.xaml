<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour" Height="auto" Width="auto">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10, 50, 10, 0">
            <Button Content="start" Command="{Binding StartCommand}" Margin="10" Width="75" Height="35" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16"/>
            <Button Content="stop" Command="{Binding StopCommand}" Margin="10" Width="75" Height="35" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10, 0, 10, 50">
            <Button Content="add ball" Command="{Binding AddBallCommand}" Margin="10" Width="125" Height="35" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16"/>
            <Button Content="remove ball" Command="{Binding RemoveBallCommand}" Margin="10" Width="125" Height="35" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16"/>
            <StackPanel DockPanel.Dock="Left" Orientation="Vertical" HorizontalAlignment="Right" Margin="10">
                <TextBlock Text="number of balls" VerticalAlignment="Center" Margin="5" FontSize="16"/>
                <TextBox Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}" Width="50" Height="25" Margin="5" TextAlignment="Center" FontSize="16"/>
            </StackPanel>
        </StackPanel>
        <Border BorderThickness="4" BorderBrush="Purple" Height="420" Width="400" Margin="25, 0, 25, 0">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width= "{Binding Diameter}" Height="{Binding Diameter}" Fill="HotPink"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </DockPanel>
</Window>